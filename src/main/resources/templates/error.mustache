<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Error - Collectibles Store</title>
  <style>
    body { font-family: Arial, sans-serif; max-width: 600px; margin: 100px auto; padding: 20px; text-align: center; }
    .error-container { background: #f8d7da; border: 1px solid #f5c6cb; padding: 30px; border-radius: 8px; }
    h1 { color: #721c24; }
    p { color: #721c24; }
    a { display: inline-block; margin-top: 20px; padding: 10px 20px; background: #007bff; color: white; text-decoration: none; border-radius: 4px; }
    a:hover { background: #0056b3; }
  </style>
</head>
<body>
  <div class="error-container">
    <h1>⚠️ {{error}}</h1>
    <p>{{message}}</p>
    {{#details}}
    <p style="font-size: 12px; color: #666;">Details: {{details}}</p>
    {{/details}}
    {{#path}}
    <p style="font-size: 12px; color: #666;">Path: {{path}}</p>
    {{/path}}
    <a href="/items">Go to Home</a>
  </div>
</body>
</html>